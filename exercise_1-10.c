// Exercise 1-10.
// Write a program to copy its input to its output, replacing each tab by
// 	, each backspace by , and each backslash by \. This makes tabs and
// backspaces visible in an unambiguous way.

#include <stdio.h>

int main()
{
    fprintf(stderr, "not yet done\n");
    return 1;
}
